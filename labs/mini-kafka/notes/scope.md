# mini Kafka Scope

## Goal

Understand how a partitioned append-only log exposes ordered reads, offset-based consumption, and leader-based replication.

## First milestone

Implement a single-node version before adding replication or consumer group coordination.
