# mini LSM KV Store Scope

## Goal

Understand the write path, flush path, and compaction model behind LSM-based storage engines.

## First milestone

Implement WAL plus memtable plus a single SSTable format before attempting compaction.
