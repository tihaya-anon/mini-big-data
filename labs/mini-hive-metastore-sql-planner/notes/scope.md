# mini Hive Metastore + SQL Planner Scope

## Goal

Understand the separation between metadata management and query planning.

## First milestone

Model databases, tables, and partitions before adding SQL parsing and plan generation.
